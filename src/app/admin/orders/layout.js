export const metadata = {
  title: "Admin: Orders",
  description: "Manage and view all customer orders in the admin dashboard",
};

export default function AdminOrdersLayout({ children }) {
  return <>{children}</>;
}
