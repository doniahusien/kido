export const metadata = {
  title: "التصنيفات",
  description: "إدارة وعرض جميع تصنيفات المتجر في لوحة التحكم",
};

export default function AdminCategoriesLayout({ children }) {
  return <>{children}</>;
}
